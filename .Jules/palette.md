## 2024-05-22 - [Modal Accessibility Improvements]
**Learning:** Adding simple accessibility attributes (`role="dialog"`, `aria-modal`) and focus management (`useEffect` for focus and Escape key) significantly improves the modal experience for keyboard and screen reader users without requiring complex libraries.
**Action:** Always include basic focus management and aria roles in custom modal components.
