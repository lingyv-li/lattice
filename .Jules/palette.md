## 2026-01-28 - Semantic Buttons
**Learning:** `div` with `role="button"` can be safely refactored to `<button>` in this codebase using `w-full text-left` to reset styles, providing free accessibility wins.
**Action:** Prefer `<button>` for all interactive card-like elements.
